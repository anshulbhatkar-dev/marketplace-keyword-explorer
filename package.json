{
  "name": "marketplace-keyword-explorer",
  "version": "1.0.0",
  "private": true,
  "description": "Marketplace Keyword Explorer — a common repository for marketplace keyword-opportunity tooling. Currently: Kw Explorer + AZ->BK Gaps. Single-file static app, data from Google Sheets, no backend.",
  "scripts": {
    "dev": "npx serve . -l 3000"
  }
}
